codecov:
  notify:
    require_ci_to_pass: yes

coverage:
  status:
    project:
      core:
        target: 70%
        threshold: 1%
        paths:
          - "!cmd/radikron-gui/"
      gui-backend:
        target: 20%
        threshold: 1%
        paths:
          - "cmd/radikron-gui/"
    patch:
      default:
        target: 70%
        threshold: 1%
